import format from "pg-format"
import { iUserRequest } from "../../interfaces/user.interfaces"

const updateUserService =async (userData: iUserRequest, userId: number) => {

    const queryString: string = format(`
    
    
    `)
    
}

export {updateUserService}